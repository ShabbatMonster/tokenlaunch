{
  "name": "tokenlaunch",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/main.js --bundle --minify --format=esm --outfile=docs/app.js && esbuild src/fees.js --bundle --minify --format=esm --outfile=docs/fees.js",
    "dev": "esbuild src/main.js --bundle --format=esm --outfile=docs/app.js --watch"
  },
  "dependencies": {
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.2"
  }
}
